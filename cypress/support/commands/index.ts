// Point d'entrée des custom commands (L2). Une responsabilité par fichier —
// voir docs/ARCHITECTURE.md §3. Les commandes héritées de l'upstream
// (getBySel, loginByApi, loginByXstate…) sont reconstruites typées en
// semaines 2 et 3 ; récupérables via `git show upstream/develop:cypress/support/commands.ts`.
export {};
